#Tuple is used to store multiple values in single variable. The key difference between the tuples and lists is that while the tuples are immutable objects the lists are mutable. This means that tuples cannot be changed while the lists can be modified.

colors = ("red", 3, 4)

for x in colors:
    print(x)
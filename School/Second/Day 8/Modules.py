#Module is the division of large chunk of code into related code. It is useful for resusability of code. We can update value in one place and it's reflected into all places.
#It is collection of related code.
from Constants import PI, GRAVITY

print(PI*4*4)
print(GRAVITY)